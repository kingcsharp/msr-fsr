CREATE TABLE [dbo].[A_PROJECT_ALLOWED_PEOPLE] (
    [ID]         VARCHAR (50) NOT NULL,
    [PROJECT_ID] VARCHAR (50) NULL,
    [PEOPLE_ID]  VARCHAR (50) NULL,
    [DRCM]       DATETIME     NULL,
    [MODBY]      VARCHAR (50) NULL,
    CONSTRAINT [PK_A_PROJECTS_ALLOWED_PEOPLE] PRIMARY KEY CLUSTERED ([ID] ASC)
);

