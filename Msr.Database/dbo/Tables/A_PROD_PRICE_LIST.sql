CREATE TABLE [dbo].[A_PROD_PRICE_LIST] (
    [ID]             VARCHAR (50) NOT NULL,
    [HISTORY_REF_ID] VARCHAR (50) NULL,
    [DRCM]           DATETIME     NULL,
    [MODBY]          VARCHAR (50) NULL,
    [STATUS]         VARCHAR (50) NULL,
    CONSTRAINT [PK_A_PROD_PRICE_LIST] PRIMARY KEY CLUSTERED ([ID] ASC)
);

