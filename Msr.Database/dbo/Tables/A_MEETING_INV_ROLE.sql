CREATE TABLE [dbo].[A_MEETING_INV_ROLE] (
    [MEETING_ID] VARCHAR (50) NULL,
    [ROLE_ID]    VARCHAR (50) NULL,
    [MODBY]      VARCHAR (50) NULL,
    [DRCM]       DATETIME     NULL,
    [ID]         VARCHAR (50) NOT NULL,
    [OPTIONAL]   SMALLINT     NULL,
    CONSTRAINT [PK_A_MEETING_INV_ROLE] PRIMARY KEY CLUSTERED ([ID] ASC)
);

