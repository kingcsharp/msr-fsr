@model Msr.Services.Workflows.ViewModels.SubmitWorkflowViewModel
@{
    ViewBag.Title = "Submit Approval Workflow";
}

<div class="container-fluid">
    <div class="side-body padding-top">
        <div class="row">
            <h2 class="page-header">
                Select Approval Workflow & Submit
                <div class=" pull-right" data-toggle="modal">
                </div>
            </h2>
            <div class="col-lg-10">
                <section class="panel">
                    <div class="panel-body bio-graph-info">
                        @Html.Partial("Message")
                        @using (Html.BeginForm("Submit", "Workflow", FormMethod.Post, new { @class = "form form-horizontal" }))
                        {
                            @Html.AntiForgeryToken()
                            @Html.HiddenFor(model => model.ObjectId)
                            @Html.HiddenFor(model => model.CompletionStart)
                            @Html.HiddenFor(model => model.ReturnUrl)

                            <div class="form-group">
                                @Html.LabelFor(model => model.Name, htmlAttributes: new { @class = "control-label col-md-2" })
                                <div class="col-md-8" style="margin-top: 7px; font-weight: 700">
                                    @Model.Name
                                    @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
                                </div>
                            </div>

                            <div class="form-group">
                                @Html.LabelFor(model => model.Comment, htmlAttributes: new { @class = "control-label col-md-2" })
                                <div class="col-md-8">
                                    @Html.TextAreaFor(model => model.Comment, htmlAttributes: new { @class = "form-control ", @style = "width: 100%; height: 80px;" })
                                    @Html.ValidationMessageFor(model => model.Comment, "", new { @class = "text-danger" })
                                </div>
                            </div>

                            <div class="form-group">
                                @Html.LabelFor(model => model.ApprovalWorflowId, htmlAttributes: new { @class = "control-label col-md-2" })
                                <div class="col-md-8">
                                    @Html.DropDownListFor(model => model.ApprovalWorflowId, Model.WorkflowsList, new { @class = "form-control select", @readonly = "true" })
                                    @Html.ValidationMessageFor(model => model.ApprovalWorflowId, "", new { @class = "text-danger" })
                                </div>
                            </div>

                            <div class="form-group">
                                <div class="col-lg-offset-2 col-lg-8">
                                    <input type="submit" value="Submit" class="btn btn-success" />
                                    @if (!Model.ShowCancel)
                                    {
                                        <a href="@Model.ReturnUrl" class="btn btn-warning">Cancel</a>
                                    }
                                </div>
                            </div>

                        }
                    </div>
                </section>
            </div>
        </div>
    </div>
</div>

@section scripts
{
    @Scripts.Render("~/bundles/js/jqueryval")
}