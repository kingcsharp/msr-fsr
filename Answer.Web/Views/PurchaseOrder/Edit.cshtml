@model Msr.Services.PurchesOrder.ViewModels.NewPurchaseOrderViewModel
@{
    ViewBag.Title = "Update Purchase Order";
}
<link href="~/assets/css/bootstrap-datetimepicker.min.css" rel="stylesheet" />
<div class="container-fluid">
    <div class="side-body padding-top">
        <div class="row">
            <h2 class="page-header">
                Edit Purchase Order<button class="btn btn-info help-btn pull-right" data-toggle="modal" data-target="#wipHelpModal">Help</button>
            </h2>
            @Html.Partial("Message")
            <div class="col-lg-10">
                @using (Html.BeginForm("Create", "PurchaseOrder", FormMethod.Post, new { @class = "form form-horizontal partCreate" }))
                {
                    @Html.Partial("_Controls")
                }
            </div>
        </div>
    </div>
</div>
@section scripts
{
    @{
        var productsJoin = string.Join(",", Model.Products);
    }
    <script src="~/assets/js/moment.min.js"></script>
    <script src="~/assets/js/bootstrap-datetimepicker.min.js"></script>
    <script src="~/assets/js/grids/add-purchase-order.js"></script>
    <script src="~/assets/js/dataTables.bootstrap.min.js"></script>
    
    @Scripts.Render("~/bundles/js/jqueryval")

    <script src="~/assets/js/grids/add-purchase-order.js"></script>

    <script type="text/javascript">
        
        $(document).ready(function () {

            $('.select').select2();

            var selectProducts = $(".select-products").select2({
                tags: [],
                tokenSeparators: [','],
                multiple: true,
                allowClear: true,
                placeholder: "Select Products",
                formatNoMatches: function () {
                    return '';
                }
            });

            var values = '@productsJoin';
            var selectedProducts = values.split(",");

            selectProducts.val(selectedProducts).trigger("change");

        });

    </script>
}